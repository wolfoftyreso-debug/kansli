# Packages (shared, optional)

Future shared packages (e.g. `@kansli/ui`, `@kansli/logging`, `@kansli/contracts`) can be introduced here **only** when it provides clear value.

No product should become dependent on **all** other products.

