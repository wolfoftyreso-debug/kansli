{
  "name": "@pixdrift/rita-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "RITA analysis-engine boundary. The real engine is the Rust binary (skattjakt); this package is the only place the product talks to it. Production uses HttpAnalysisEngine — never FakeAnalysisEngine.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.1.13"
  }
}
