{
  "name": "@pixdrift/ai-core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pixdrift AI Core — one unified model API over Anthropic/OpenAI/Gemini (and an optional AI gateway), with failover, provenance and constitution guardrails (output is always inference, never fact).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20"
  }
}
